# SOW: арка «EM-стационар + warm-start» — ТЗ и ставка, заморожены ДО правок

*17.07.2026. Оркестровка: диагностика — три независимых агента (Sonnet),
ТЗ/ставка — арбитр (Fable), реализация — Sonnet, ревью — Opus, вердикт —
арбитр. Ставка объявлена ДО каких-либо правок ядра.*

## 1. Диагноз (три отчёта, сведены)

1. **Недосхождение EM доказано прямым экспериментом.** `fit_axis`
   (organism2.py:632) обрезает EM жёстким `iters=5` без критерия
   сходимости. Та же ось (тумблер 6/18, compose_toggle), те же данные:
   5 итераций → gap 0.18 (36% теории), 20 → 0.42, 50 → **0.493 = 98.6%**,
   дальше плато (остаток — конечная выборка, 948 наблюдений).
   Вырожденные роли колец/тумблеров и «карта-монета» batch-07 — отсюда.
   «Голод меньшинства» — на деле РАЗДУВАНИЕ меньшинства (+72…135%):
   EM не успевает опустить малую роль к стационару.
2. **«Лотереи EM-инициализации» НЕ существует.** Гипотеза прошлой смены
   опровергнута: разброс битов k=2-фита по 16 сидам — 0.8–6.6 бит,
   k=2 выигрывает у k=3 локально 16/16 на любом объёме данных. Числа
   45.8/164.7 из старой гипотезы не воспроизводятся.
3. **Регресс flashing (5/7 → 3-4/7) — хрупкий глобальный перевес.**
   После арки открытия (3844b10) судья видит k=3; в сне 10 тот
   выигрывает **0.70 бита** (123.92→123.22) — не HMM-фитом (он хуже),
   а чуть более компактным ruleset. Принятый k=3 даёт загрязнённые
   кластеры (56–73% смешения) и роняет экзамен. Гипотеза-связка: биты
   недосошедшего k=2 завышены — при сошедшемся EM перевес k=3 исчезает.
4. **Warm-start отсутствует полностью** — каждый фит с нуля
   (рестарт 0 — кластеры со-подписей, рестарт 1 — случайный);
   параметры оси-чемпиона никуда не передаются.

## 2. ТЗ (правки ТОЛЬКО в fit_axis и прокидке аргумента)

1. **EM-стационар:** цикл EM в `fit_axis` завершается по сходимости
   `|Δll| < 1e-9·max(1,|ll|)` (порог машинного класса, как tol=1e-12
   в `stationary()` — не подгоночная константа); потолок итераций 60 —
   страховка времени (плато по данным диагностики ~50).
2. **Warm-start:** параметр `warm_ax` в `fit_axis` — параметры
   оси-чемпиона того же (набор действий, k) добавляются ТРЕТЬИМ
   стартовым кандидатом к кластерному и случайному (не заменяют их).
   Прокидка из `_fit_block`/`_fit_block_min2`/`fit_k` (чемпион ищется
   в self.axes по frozenset(actions) и совпадению k).
3. **Судью не трогаем.** `_total_bits`, `_group_resid`, `_structure`,
   парк — без изменений. Детерминизм сохраняется (критерий сходимости
   детерминирован, rng не добавляется).

## 3. Ставка (гейты после реализации)

- **С1.** flashing возвращается к 5/7 с осью k=2 (перевес k=3 исчезает
  при сошедшемся EM). Протокол замера: `python3 runworld2.py
  worlds/last/flashing.yaml`, бюджет 2000; базлайн HEAD снимается
  тем же протоколом в той же смене (узел 4/7-vs-3/7 закрывается).
- **С2.** compose_toggle: выученный gap ≥ 80% теоретического на всех
  точках с асимметрией ≥ 8/16; немонотонность по бюджету остаётся
  только там, где gap_теор ≈ 0 (там она честная).
- **С3.** Не хуже: T1 6/7 (узел hard3=ОТКАЗ), drop 6/7, bact 7/8,
  baloon1/2 8/8, hard1 ≤ 85.1 бит, hard3-донор k=4, b06-доноры
  m03/m04 держат k=3, probe_f2 / немота — бит-в-бит.
- **С4. Цена времени:** прогоны гейтов дорожают не более чем ×3.

## 4. Проигрышные клетки (объявлены заранее)

- flashing НЕ вернулся при сошедшемся EM → корень не недосхождение,
  а хрупкость судьи на марже < 1 бита → отдельная арка
  «маржа/гистерезис судьи»; подкрутка текущей арки ЗАПРЕЩЕНА.
- Доноры b06 теряют k=3 → открытие колец держалось на недосхождении →
  арка в текущем виде не принимается.
- Гейт времени провален → cap/оптимизация отдельно, пересдача гейтов.

## 5. После зелёных гейтов: пересев полки и прогон batch-08 (решение Кирилла 17.07)

1. **Пересев полки новым ядром:** полка строится заново — архетипы
   засеваются пост-фиксовым ядром, доноры с полки переоткрываются из
   их origin-миров тем же протоколом приёмки (парный тест, дрожь,
   карантин вторым сидом). Старая полка v6 остаётся в git; версия
   полки штампуется в строки ведомости.
2. **Один прогон batch-08, два ядра:** базлайн — worktree a61e671
   (ядро и полка v6 как в git), пост-фикс — текущее ядро (открытие
   колец 3844b10 + эта арка) с пересеянной полкой. Оба прогона —
   в exams_ledger.jsonl.
3. **Вскрытие запечатанного замысла (ключ вне репо)** — только после
   вердиктов; ставка a61e671 судится по вскрытию. Честная оговорка в
   ведомость: ставка заявляла пост-фикс как «ворота + роль-профиль»,
   фактический пост-фикс — «открытие колец + EM-стационар/warm-start»;
   ставку задним числом не переписываем, расхождение фиксируем.
