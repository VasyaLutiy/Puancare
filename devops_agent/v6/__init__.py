"""
devops_agent.v6 — фаза РАСПОЗНАВАНИЯ и РАЗМЕЩЕНИЯ сущностей (база знаний агента).

Поток: снимок мира → NLU(LLM) превращает сырьё в JSON-фрагмент (6 мета-типов) →
агент СТРУКТУРНО распознаёт (известная категория по сигнатуре | новое) → кладёт в DAG.
Без PDDL. Валидатор = контракт формы + DAG-консистентность (ацикличность) + (мир — позже).

ЛИНИЯ (иначе LLM-фронтенд): LLM = восприятие БЕЗ состояния (сырьё→JSON, не видит граф);
агент = память + структурное распознавание (детерминированно, по сигнатуре). JSON-схема — мембрана.

Метрика «учится» (поправка E1): не «LLM→0», а число КАТЕГОРИЙ выходит на ПЛАТО при
растущем потоке инстансов, доля распознанного → высокая. Всё видно под капотом (hood).

Модули: world (поток фрагментов) · recognizer (сигнатура+распознать+DAG) · hood (трейс+метрики) ·
nlu (LiveNLU: сырьё→JSON живым Azure) · run (демо объёма + живой smoke).
"""
