# RESULTS.md — честный протокол эксперимента (Шаги 0–B)

> Все числа — боевые прогоны на VPS (Python 3.14.4), детерминированные пары (SEED=0).
> Теория и пред-регистрация — в `ResearchPuancare.md`. Формула стоимости заморожена
> в git (Шаг 2) ДО любого внешнего сигнала — пред-регистрация соблюдена.

---

## Сводка одним абзацем

Внутренняя конструкция (Шаги 0–2) чиста и воспроизводит founding-феномен «это»
по построению. Внешняя проверка (Шаги 3, 3b, A, B) даёт **скромный, но устойчивый
к контролю частоты** сигнал: направленный потенциал `h_ic` предсказывает
**направление** асимметрии верификации лучше частоты и симметричных бейзлайнов,
и заметно сильнее — **на парах, где соблазн ошибки реально есть** (Spearman ~0.42).
Но эффект малый и хрупкий, а **вторая ось (поверхностное сходство) необходима и пока
не операционализирована** (overlap глоссов слишком разрежен). Теория **не убита и не
доказана** — она частично подтверждена и указала на свой собственный недостающий узел.

---

## Шаг 0 — TDA-гейт ✅

`animal.n.01`: **V=3999, E=4033, β₀=1, β₁=35 (0.88% от V)**.
Дерево топологически почти чисто → строить `h` можно. Все 35 петель = множественное
наследование одного типа: вторая ось поперёк таксономии (`food_fish`, `male`, `cub`,
`racer`). Гейт не просто пропустил — **разметил, где одного скаляра не хватит.**

## Шаг 1 — потенциал `h` ✅

Согласованность потенциала на 4033 рёбрах `is-a` (должно быть `h(child)>h(parent)`):

| потенциал | согласованность | нарушения |
|---|---|---|
| `h_depth` (Poincaré-радиус) | 99.18% | 33 — **все 33 на loop-узлах β₁** |
| `h_ic = −log₂(|desc|/V)` | **100.00%** | 0 |

Path-based потенциал ломается ровно на петлях Шага 0; measure-based — нигде.
**Несущий потенциал = `h_ic`** (он же `log(N)` для формулы стоимости).

## Шаг 2 — формула стоимости ✅ (по построению)

`cost(A→B) = h_ic(B) − h_ic(LCA(A,B))`. Обобщение бесплатно на **всех 4033 рёбрах**;
конкретизация: медиана **2.81 бит**, среднее 3.28. Founding-феномен численно:

| вопрос | cost | вердикт |
|---|---|---|
| `щука это рыба?` | 0.00 | спонтанное «да» |
| `кит это млекопитающее?` | 0.00 | спонтанное «да» |
| `кит это рыба?` | **2.32** | «нет» + напряжение (LCA уехал до `vertebrate`) |

Это внутренняя согласованность, **не доказательство связи с реальностью.**

---

## Шаг 3 — KILL-TEST против GPT-2 logprob (N=240) ⚠️ прошёл слабо

Независимый сигнал: `logP("A is a B") − logP("B is a A")`, GPT-2, корпусный.

| предиктор | Spearman с GPT-2 |
|---|---|
| **НАШ `h_ic`-diff** (направленный) | **+0.148** |
| `freq_diff` (направленный) | +0.091 |
| `path_len` (симметричный) | −0.130 ≈ структурно 0 |
| `emb_cos` (симметричный) | +0.018 ≈ структурно 0 |

sign-match: наш **57.5%** vs частота 46.2% (chance=50%).
**Критерий убийства: partial Spearman(наш, GPT-2 | freq) = +0.248 → ВЫЖИЛ** (порог 0.1).

Значимо (N=240, σ≈0.065), но **малая магнитуда**. Симметричные бейзлайны провалились
на направлении — как и заявлено. Частота наш сигнал не объясняет.

## Шаг 3b — фронтир-подтверждение (gpt-5.4-mini, live, N=120) ⚠️ на грани

Сигнал: градуированная правдоподобность категориальной ОШИБКИ в обе стороны.

| предиктор | Spearman | partial \| freq |
|---|---|---|
| **НАШ `h_ic`-diff** | +0.226 | **+0.153** (едва выжил) |
| sign-match | 45.5% | (ниже монетки!) |

На случайных парах фронтир-результат **пограничный** и хуже GPT-2. **Два судьи
разошлись** (GPT-2 partial +0.248 vs фронтир +0.153) → сигнал хрупкий, зависит от судьи.

## Шаг A — диагностика: провал = шум измерения ✅ важно

**74% случайных cousin-пар имеют salience < 20** — соблазна нет НИ в какую сторону
(две безвестные рыбы), асимметрия = чистый шум. Пере-тест по порогу соблазна:

| salience ≥ T | n | Spearman(наш, asym) | partial\|freq |
|---|---|---|---|
| 0 (все) | 120 | +0.226 | +0.153 |
| **10** | 62 | **+0.416** | **+0.282** |
| 20 | 31 | +0.430 | +0.158 |
| 30 | 22 | +0.448 | +0.125 |

**На парах с реальным соблазном raw-корреляция прыгает 0.23 → 0.42–0.45.**
Провал 3b был **в основном шумом измерения, а не смертью теории**: на 74% мёртвых пар
предсказывать нечего. partial|freq фрагилен на малых n (падает к 0 при T≥40, n<20).

## Шаг B — вторая ось (overlap глоссов) ❌ реализация слаба

| предиктор | Spearman | partial\|freq |
|---|---|---|
| `h_ic`-diff (1 ось) | +0.226 | +0.180 |
| `gloss_sim` (2 ось, симм.) | +0.185 | — |
| **COMBINED = h_ic·gloss** | **+0.048** | +0.019 ❌ |

Мультипликативная связка **ухудшает**: глоссы WordNet слишком разрежены. Даже у
`whale/fish` gloss_sim=0.10 — определения не делят слова «вода/обтекаемый», хотя
фронтир это сходство явно использует (классика ниже). **Вторая ось нужна
(подтверждено), но glosses — плохой её носитель.**

Классика (live, фронтир уверенно асимметричен, наш знак совпал на всех явных):

| пара | asym (фронтир) | h_ic-diff | gloss |
|---|---|---|---|
| whale→fish | 91 | +4.27 | 0.10 |
| dolphin→fish | 91 | +7.69 | 0.04 |
| penguin→bird | 93 | +7.18 | 0.08 |
| bat→bird | 77 | +4.41 | 0.14 |
| seal→fish | 76 | +5.10 | 0.05 |
| shark→whale | −6 | −0.32 | 0.00 |

---

## Что мы поняли (двухфакторная модель)

1. **Сходство → ЕСТЬ ли соблазн** (магнитуда). Низкое сходство → нет ошибки ни в какую
   сторону (74% пар). Это гейт.
2. **Абстракция-асимметрия `h_ic` → КУДА соблазн** (направление). Внутри соблазнительных
   пар `h_ic`-diff предсказывает направление при raw Spearman ~0.42.

Шаг A фактически использовал salience самого фронтира как гейт сходства и показал, что
`h_ic` ловит направление. Шаг B провалился только потому, что glosses — плохой
измеритель сходства, **не потому что двухфакторная модель неверна.**

## Вердикт (без натяжки)

- **Не убито, не доказано.** Частично подтверждено: направленный потенциал несёт
  реальный, контролируемый по частоте сигнал о направлении асимметрии, особенно там,
  где соблазн реален. Эффект мал и хрупок между судьями.
- **Главная дыра — вторая ось (поверхностное сходство).** Её предсказал ещё Шаг 0
  (β₁-петли). Нужен носитель лучше глоссов.

## Открытые ходы

- **Лучшая ось сходства**: меронимы/атрибуты, признаковые нормы (McRae), или
  гибрид (эмбеддинг-косинус как гейт — §7 разрешает) — и честно сравнить вклад.
- **Аддитивная**, а не мультипликативная связка осей + регрессия.
- **Больше соблазнительных пар** (целевой сэмплинг high-similarity) — снять
  недо-мощность, увидеть устойчивость partial|freq на n>60.
